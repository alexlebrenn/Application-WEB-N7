<%= form_for([@comment.post, @comment]) do |f| %>
    <div class="field">
    <%= f.label :Auteur %><br />
    <%= f.text_field :author %>
  </div>
  <div class="field">
    <%= f.label :Contenu %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <br><%= f.submit %>
  </div>
<% end %>
